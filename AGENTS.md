# AGENTS.md

## 产品

班主任 AI 工作台面向高职 / 大专班主任、辅导员和学工教师。它是班主任个人的本地工作层，负责日常发现、过程跟进、本地归档和材料整理；学校学工系统负责正式审批、上报和统一留痕。

## 不得违背的原则

### Local First

- 原始学生数据、花名册、身份映射、历史事件、待办和谈话默认保存在本机浏览器。
- 不增加后端数据库、登录、云同步、遥测或埋点。
- 能用本地 JavaScript 完成的处理必须在本地完成。
- 除教师主动打开外部 AI 外，主要功能在网络不可用时仍应可用。

### Privacy by Design

本地页面可以显示完整资料。任何可能包含学生信息的 AI 操作必须经过：任务判定 → 最小必要 → 去标识化 / 泛化 → 人工确认 → AI → 本地恢复身份 → 人工审核。

不得承诺“完全匿名”或“绝对安全”。身份证、手机号、住址、宿舍、宗教和性取向默认不得进入 AI payload；家庭和心理信息只有与任务确有关系时才可泛化后使用。

### Human in the Loop

自动处理后必须显示本地原文、AI 实际 payload、识别字段、替换原因和人工编辑操作。未经最终确认不得继续，AI 返回未经本地恢复和人工审核不得保存。

### 稳定能力

不得无理由重构文件解析、IndexedDB 迁移、稳定 S 代号、脱敏 detector、Privacy Gateway、PWA、事件 / 待办 / 谈话关联和测试框架。数据模型升级必须提供兼容迁移，不得清空既有数据。

## 产品与 UI

- 首页是班主任工作台，不是聊天页或 AI 功能门户。
- UI 专业、克制、桌面端优先，不使用蓝紫渐变或夸张科技感。
- 心理 / 医疗相关功能只记录事实、整理输入和形成跟进，不诊断、不预测、不贴风险标签。
- 不开发语音识别，不自动写入学工系统，不默认调用第三方 AI。

## 维护要求

- 修改前阅读 `docs/HANDOFF.md`。
- 保持 `/workbench` 和 `/` 为正式入口，演示入口为 `/workbench?mode=demo`。
- 使用虚构数据编写测试，禁止把真实学生资料提交到仓库。
- 每轮修改执行相应单元测试、Chromium 流程、生产构建；涉及兼容性时执行 WebKit。
- 更新 `docs/PROGRESS.md`，写明完成项、验证结果、迁移影响和已知问题。
